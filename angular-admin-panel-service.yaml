apiVersion: v1
kind: Service
metadata:
  name: sit-fe-admin-panel-svc
  namespace: sit
  labels:
    app: sit-fe-admin-panel
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: sit-fe-admin-panel
    tier: frontend
  type: LoadBalancer