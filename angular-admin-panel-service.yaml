apiVersion: v1
kind: Service
metadata:
  name: fe-admin-panel
  namespace: production
  labels:
    app: fe-admin-panel
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: fe-admin-panel
    tier: frontend
  type: LoadBalancer