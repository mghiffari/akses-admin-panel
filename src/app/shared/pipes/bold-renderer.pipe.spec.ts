import { BoldRendererPipe } from './bold-renderer.pipe';

describe('BoldRendererPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldRendererPipe();
    expect(pipe).toBeTruthy();
  });
});
